# The demo app is in-memory and needs no real config.
# These are placeholders to illustrate the pattern — copy this file
# to .env and override locally if you change the app.

LOG_LEVEL=info
PORT=8000
